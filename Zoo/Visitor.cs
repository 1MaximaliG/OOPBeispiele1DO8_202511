namespace Zoo
{
    public class Visitor
    {
        protected int _anzahl=0;
        protected decimal _eintrittspreis = 99;
        public int Anzahl { get { return _anzahl; }}
        public decimal Eintrittspreis { get { return _eintrittspreis; }}
    }
}
